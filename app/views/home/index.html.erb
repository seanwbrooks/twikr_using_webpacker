<% if user_session %>
  <div id="app"></div>
<% else %>
  <div class="home">
    <div class="card">
      <div class="row">
        <h1>TWIKR</h1>
        <h3>Twikr is a financial commmunications platform.</h3>
      </div>
      <div class="row">
        <h4>Share your ideas. Follow stocks.</h4>
      </div>
      <div class="row">
        <a href="<%= new_user_session_path %>" class="button small radius">Sign In</a>
      </div>
    </div>
  </div>
<% end %>
